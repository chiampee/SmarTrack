# FastAPI framework
fastapi>=0.109.1
uvicorn[standard]==0.27.0
python-multipart>=0.0.18
gunicorn>=22.0.0

# MongoDB (motor is async, pymongo for sync)
motor==3.6.0
pymongo==4.9.1

# Auth0
python-jose[cryptography]>=3.4.0
python-dotenv==1.0.0

# Content extraction (simplified - no lxml)
beautifulsoup4==4.12.2
html5lib==1.1
requests>=2.32.4

# Data validation (latest stable with wheels)
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.1.0

# Testing
pytest==7.4.3
httpx==0.26.0

# Security Fixes - Transitive dependencies pinned to secure versions
# These are required to fix vulnerabilities in dependencies of dependencies
urllib3>=2.6.0  # Fixes CVE-2025-50181, CVE-2025-66471, CVE-2024-37891, CVE-2025-66418
h11>=0.16.0  # Fixes CVE-2025-43859 (request smuggling)
idna>=3.7  # Fixes CVE-2024-3651 (DoS vulnerability)
anyio>=4.4.0  # Fixes thread race condition in multi-threaded environments
certifi>=2024.0.0  # Updates CA certificate bundle
pyasn1>=0.6.2 # not directly required, pinned by Snyk to avoid a vulnerability
